{
  "name": "moral-dilemma",
  "private": true,
  "type": "module",
  "scripts": {
    "config": "node tools/write-config.mjs",
    "prestart": "npm run config",
    "start": "python3 -m http.server 5173",
    "pretest": "npm run config",
    "test": "node --test test/scoring.test.mjs test/selection.test.mjs test/smoke.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-moral-dilemma \"node --test test/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2",
    "puppeteer-core": "^25.10.0"
  }
}
